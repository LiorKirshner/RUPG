export async function fetchUserData() {
  // This will later fetch data from APIs
  // return fetch(...);
}

export class Renderer {
  renderUser(userData) {
    // This will later update the DOM with the user info
  }
}
